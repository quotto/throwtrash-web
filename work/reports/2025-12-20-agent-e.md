# 作業レポート (2025-12-20)

## 対応内容
- `api/func-template.yml` の Lambda Runtime と Layer の CompatibleRuntimes を `nodejs22.x` に更新
- `api/src/package.json` の `@types/node` を Node 22 系へ更新
- `api/src/package-lock.json` を再生成

## 実行結果
- `npm test` (api/src): 成功
- `npm run build` (api/src): 成功

## 補足
- テスト実行時に AWS SDK v2 のメンテナンスモード警告が表示された（挙動は従来通り）

---

## AWS SDK v3 リファクタ

### 対応内容
- `api/src/dbadapter.ts` を AWS SDK v3（@aws-sdk/client-dynamodb / @aws-sdk/lib-dynamodb）へ移行
- DynamoDBDocumentClient を外部注入可能にする `setDocumentClient` を追加
- テスト用に AWS 認証選択（AWS_PROFILE -> fromSSO / AWS_ACCESS_KEY_ID -> fromEnv）ロジックを追加
- `api/src/__tests__/dbadapter.test.ts` を追加（正常系/異常系）
- `api/src/package.json` / `api/src/package-lock.json` の依存関係更新

### 実行結果
- `npx eslint . --fix --ignore-pattern api-dist` (api/src): 成功
- `npm test` (api/src): 成功
- `npm run build` (api/src): 成功
