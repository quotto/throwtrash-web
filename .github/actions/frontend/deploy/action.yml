name: "Frontend Deploy (placeholder)"
description: "Amplify handles deploy via amplify.yml; no-op here"
runs:
  using: composite
  steps:
    - run: echo "Deploy handled by Amplify Hosting build (amplify.yml)"
